# Final Decision Note

## Decision

Choose **B**:

Do at most one final `graph-native consistency / consensus` experiment in the future. If it still cannot beat the matched non-graph baseline, permanently stop the Route B main-method line.

## Why Not A Right Now

Route B still has one narrow remaining question that is not yet closed:
whether multi-node consistency structure can create a gain that same-information non-graph aggregation cannot reproduce.

That is the only unresolved reason not to stop the Route B method line immediately.

## Why Not Continue More Broadly

- Held-out evidence already shows that current gains do not prove independent graph value.
- Broadening Route B now would only expand an unproven method direction.
- Route B should not move into main experiment mode under the current evidence.

## Operational Interpretation

- Main work should return to Route A.
- Route B is currently an `analysis / conflict-focused module`.
- If the single graph-native experiment is never run, Route B should remain frozen at that downgraded status.
