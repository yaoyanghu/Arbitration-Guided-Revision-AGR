project:
  name: New_ChronoRAG
  root_dir: /home/huyaoyang/Projects/flashrag_project_20251213/New_ChronoRAG
  seed: 42
  device: cpu

paths:
  data_dir: ${project.root_dir}/data
  processed_data_dir: ${paths.data_dir}/processed
  corpus_dir: ${paths.data_dir}/corpus
  index_dir: ${project.root_dir}/indexes
  runs_dir: ${project.root_dir}/runs
  log_dir: ${project.root_dir}/logs

mainline:
  dataset_name: temprageval_formal_1244
  run_stage: formal
  queries_path: ${paths.processed_data_dir}/temprageval_formal_1244.jsonl
  corpus_path: ${paths.corpus_dir}/temprageval_formal_1244_corpus.jsonl
  bm25_index_dir: ${paths.index_dir}/temprageval_formal_1244_bm25
  retrieval_top_k: 4
  evidence_top_k: 2
  bm25_weight: 0.30
  lexical_weight: 0.20
  temporal_weight: 0.25
  conflict_weight: 0.20
  structured_weight: 0.0
  reliability_weight: 0.0
  generation_mode: citation_aware
  citation_style: bracket
  enable_top_gap_filtering: false

# Ablation metadata
ablation:
  variant_name: no_top_gap_filtering
  base_config: configs/conflict_aware_rag_temprageval_formal_1244_linux.yaml
  formal_or_pilot: formal
  dataset_size: 1244
  changed_parameters_only:
    - enable_top_gap_filtering: true -> false
